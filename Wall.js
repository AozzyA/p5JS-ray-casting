class Wall {
  //I think I might need this? IDK what I'm doing
  constructor(x1, y1, x2, y2, name) {
    this.x1 = x1; //sets first coordinate point
    this.y1 = y1; // ^
    this.x2 = x2; //sets second coordinate point
    this.y2 = y2; // ^
    this.name = name;
  }
}